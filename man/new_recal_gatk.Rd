% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gatk.R
\name{new_recal_gatk}
\alias{new_recal_gatk}
\title{GATK Base Quality Score Recalibration (BQSR) workflow}
\usage{
new_recal_gatk(
  bin_samtools = build_default_tool_binary_list()$bin_samtools,
  sif_gatk = build_default_sif_list()$sif_gatk,
  bin_picard = build_default_tool_binary_list()$bin_picard,
  ref_genome = build_default_reference_list()$HG19$reference$genome,
  dbsnp = build_default_reference_list()$HG19$database$all_common,
  bam = NULL,
  ...
)
}
\arguments{
\item{bin_samtools}{Path to the \code{samtools} binary. Defaults to
\code{build_default_tool_binary_list()$bin_samtools}.}

\item{sif_gatk}{Path to the Singularity image containing GATK. Defaults
to \code{build_default_sif_list()$sif_gatk}.}

\item{bin_picard}{Path to the Picard jar. Defaults to
\code{build_default_tool_binary_list()$bin_picard}.}

\item{ref_genome}{Path to the reference genome FASTA (required for
recalibration and realignment steps).}

\item{dbsnp}{Path to known-sites VCF(s) used by \code{BaseRecalibrator}.}

\item{bam}{Path to the input BAM file to recalibrate.}

\item{...}{Additional named arguments forwarded into the internal job
environment (commonly: \code{output_dir}, \code{tmp_dir}, \code{threads}, \code{ram},
\code{mode}, \code{batch_config}, \code{verbose}, \code{clean}, etc.).}
}
\value{
Invisibly returns the job report produced by the internal job
runner and writes recalibration tables and recalibrated BAM(s) into
the configured output directory.
}
\description{
High-level wrapper that runs GATK's BaseRecalibrator and ApplyBQSR steps
following GATK best-practices. The function constructs containerized
commands (via Singularity) and submits them through the package's job
runner. It integrates with the pipeline environment and expects common
runner-managed variables (for example \code{out_file_dir}, \code{input},
\code{input_id}, and \code{fn_id}) when called via \code{launch()}.
}
\details{
The function implements a two-step BQSR process: it first calls
\code{BaseRecalibrator} to compute recalibration tables and then calls
\code{ApplyBQSR} to produce the recalibrated BAM. When invoked with a
\code{region} argument (via the runner) the function will restrict
recalibration to that interval and name outputs accordingly.
}
