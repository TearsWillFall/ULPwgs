% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util.R
\name{get_ref_from_bam}
\alias{get_ref_from_bam}
\title{Extract genome BED from a BAM header}
\usage{
get_ref_from_bam(
  bin_samtools = build_default_tool_binary_list()$bin_samtools,
  bam = NULL,
  header = TRUE,
  ...
)
}
\arguments{
\item{bin_samtools}{Path to the \code{samtools} binary. Defaults to
\code{build_default_tool_binary_list()$bin_samtools}.}

\item{bam}{Path to the input BAM/SAM file.}

\item{header}{Logical; if TRUE a column header line (\verb{chr\\\\tstart\\\\tend})
is prepended to the output BED. Default TRUE.}

\item{...}{Additional arguments forwarded to the pipeline (e.g. batch/mode,
\code{verbose}, \code{threads}, \code{ram}).}
}
\value{
Invisibly returns the job report produced by the internal job
runner and writes the genome BED file to the task output directory.
}
\description{
Read the header of a BAM/SAM file and produce a genome BED file
containing contig name, start (0) and contig length (end) for each
sequence record found in the header. The BED file is written to the
function's \code{out_file_dir} using the name \verb{<input_id>.genome.bed}.
}
