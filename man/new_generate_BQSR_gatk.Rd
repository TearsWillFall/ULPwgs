% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gatk.R
\name{new_generate_BQSR_gatk}
\alias{new_generate_BQSR_gatk}
\title{Generate GATK BaseRecalibrator (BQSR) table}
\usage{
new_generate_BQSR_gatk(
  sif_gatk = build_default_sif_list()$sif_gatk,
  ref_genome = build_default_reference_list()$HG19$reference$genome,
  dbsnp = build_default_reference_list()$HG19$database$all_common,
  bam = NULL,
  region = NULL,
  ...
)
}
\arguments{
\item{sif_gatk}{Path to the GATK Singularity image. Default build_default_sif_list()$sif_gatk.}

\item{ref_genome}{Path to the reference genome FASTA. Default build_default_reference_list()$HG19$reference$genome.}

\item{dbsnp}{Known-sites VCF(s) used by BaseRecalibrator. May be a vector. Default build_default_reference_list()$HG19$database$all_common.}

\item{bam}{Input BAM file to recalibrate. If missing, the function expects a value passed into the internal environment as \code{input}.}

\item{region}{Optional region string to restrict processing (e.g. "chr1:1000-2000"). If provided, the job will be limited to this region.}

\item{...}{Additional arguments forwarded to the internal launch environment (e.g. \code{tmp_dir}, \code{out_file_dir}, \code{executor_id}, \code{batch_config}, \code{threads}, \code{ram}, \code{mode}, \code{time}, \code{update_time}, \code{wait}, \code{hold}, \code{verbose}).}
}
\value{
A job report list. The generated recalibration table path is available in \code{out_files$recal_table}.
}
\description{
Runs GATK BaseRecalibrator inside a Singularity image to produce a BQSR
recalibration table from an input BAM and known-sites VCF(s). Optionally the
operation can be restricted to a genomic region. Additional execution
parameters (tmp dirs, batch settings, threads, ram, etc.) are passed via
the variadic arguments and handled by the internal launch environment.
}
\details{
The function builds and runs a command equivalent to:
\code{singularity run <sif_gatk> /gatk/gatk BaseRecalibrator -I <bam> -R <ref_genome> --known-sites <dbsnp> -O <out.recal.table>}.
Multiple known-sites VCFs are passed as repeated \code{--known-sites} arguments.
}
