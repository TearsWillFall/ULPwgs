% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gatk.R
\name{new_apply_BQSR_gatk}
\alias{new_apply_BQSR_gatk}
\title{Apply GATK Base Quality Score Recalibration (BQSR)}
\usage{
new_apply_BQSR_gatk(
  sif_gatk = build_default_sif_list()$sif_gatk,
  ref_genome = build_default_reference_list()$HG19$reference$genome,
  dbsnp = build_default_reference_list()$HG19$database$all_common,
  bam = NULL,
  region = NULL,
  rec_table = NULL,
  ...
)
}
\arguments{
\item{sif_gatk}{Path to the Singularity image for GATK. Defaults to
\code{build_default_sif_list()$sif_gatk}.}

\item{ref_genome}{Reference genome fasta path. Defaults to
\code{build_default_reference_list()$HG19$reference$genome}.}

\item{dbsnp}{Path to dbSNP VCF (used by other BQSR steps). Defaults to
\code{build_default_reference_list()$HG19$database$all_common}.}

\item{bam}{Path to the input BAM to recalibrate.}

\item{region}{Optional region string (e.g. "chr1:10000-20000"). If
provided the command will limit processing to this interval.}

\item{rec_table}{Path or vector of recalibration table(s). When \code{region}
is given a matching entry will be looked up from \code{rec_table}.}
}
\value{
Invisibly returns the job report created by the internal job
runner and writes the recalibrated BAM (and/or recalibration table)
to the task output directory.
}
\description{
Run GATK's \code{ApplyBQSR} using a Singularity image. The function builds
the container command to apply a recalibration table to a BAM file and
writes the recalibrated BAM to the task output directory. When \code{region}
is provided the function will apply BQSR to the specified region and
name the output accordingly.
}
