% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/picard.R
\name{new_gather_bam_files_picard}
\alias{new_gather_bam_files_picard}
\title{Gather BAM files with Picard}
\usage{
new_gather_bam_files_picard(
  bin_picard = build_default_tool_binary_list()$bin_picard,
  bam = NULL,
  output_name = "Sample",
  clean = TRUE,
  ...
)
}
\arguments{
\item{bin_picard}{Path to the Picard jar. Defaults to
\code{build_default_tool_binary_list()$bin_picard}.}

\item{bam}{Character vector of BAM paths to gather.}

\item{output_name}{Base name for the output BAM file. Default "Sample".}

\item{clean}{Logical; when TRUE intermediate input files (and their
auxiliary files) will be removed after successful gather. Default TRUE.}

\item{...}{Additional arguments forwarded into the internal job
environment (e.g. \code{output_dir}, \code{mode}, \code{batch_config}, \code{threads},
\code{ram}, \code{verbose}).}
}
\value{
Invisibly returns the job report produced by the internal job
runner and writes the gathered BAM to the task output directory as
\verb{<input_id>.bam}.
}
\description{
Wrapper that builds and runs a Picard \code{GatherBamFiles} command using the
provided Singularity/Java Picard jar. Intended to collect scattered
BQSR/ApplyBQSR outputs into a single BAM for the sample. The function
constructs the command, optionally removes intermediate files when
\code{clean = TRUE}, and submits the work through the package job runner.
}
