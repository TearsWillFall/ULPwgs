% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gatk.R
\name{new_analyze_covariates_gatk}
\alias{new_analyze_covariates_gatk}
\title{Run GATK AnalyzeCovariates to compare BQSR covariates}
\usage{
new_analyze_covariates_gatk(
  sif_gatk = build_default_sif_list()$sif_gatk,
  before = NULL,
  after = NULL,
  output_name = "sample",
  ...
)
}
\arguments{
\item{sif_gatk}{Path to the Singularity image for GATK. Defaults to
\code{build_default_sif_list()$sif_gatk}.}

\item{before}{Path to the pre-recalibration table (output of
\code{BaseRecalibrator}) for the "before" comparison. Optional.}

\item{after}{Path to the after-recalibration table for the "after"
comparison. Optional.}

\item{output_name}{Base name used for output files. Defaults to
"sample".}

\item{...}{Additional arguments forwarded into the internal job
environment (for example \code{output_dir}, \code{tmp_dir}, \code{mode},
\code{batch_config}, \code{threads}, \code{ram}, \code{verbose}).}
}
\value{
Invisibly returns the job report created by the internal job
runner and writes PDF and CSV outputs to the task output directory.
}
\description{
Wrapper for GATK's \code{AnalyzeCovariates} that runs inside the project's
Singularity image. Use \code{before} and/or \code{after} to provide the
pre- and after-recalibration tables (from \code{BaseRecalibrator}); the
function will create PDF plots and CSV summaries in the task output
directory. If only \code{before} or \code{after} is provided, the function will
produce the corresponding single plot; if both are provided, a
before/after comparison is produced.
}
